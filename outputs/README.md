# Outputs

This directory contains the raw outputs generated by the language model before evaluation.

Examples include:

- Raw model responses
- Extracted MITRE ATT&CK technique IDs
- Structured outputs (e.g., JSON)
- Intermediate files produced during the evaluation pipeline

These files serve as intermediate artifacts between model inference and final evaluation, making it easier to inspect model behavior and reproduce the analysis.
